# 项目：2026 美加墨世界杯中文资讯站

## 一、项目目标
做一个面向中文用户的 2026 FIFA 世界杯资讯站，核心目标是**搜索引擎自然流量**。
内容板块：赛程、夺冠/比分预测、开赛前瞻、球队介绍、球员分析。
站点要在世界杯开幕前先上线骨架并被搜索引擎收录，开赛后持续更新。

## 二、受众与语言
- 受众：以中文用户为主。
- 界面与内容默认简体中文；专有名词（球队、球员、城市）可保留英文原名作为辅助。

## 三、技术栈（不要擅自更换）
- 框架：Next.js（App Router）+ TypeScript
- 渲染：以静态生成（SSG / generateStaticParams）为主，保证 SEO 与首屏速度
- 样式：Tailwind CSS
- 数据来源：本地 `data/` 目录下的 JSON / Markdown 文件驱动，**不要把数据写死在组件里**
- 部署目标：Vercel（保证可一键部署、改数据后快速重新发布）
- 包管理：pnpm

## 四、信息架构（路由）
- `/`                      首页：揭幕战倒计时、近期/热门比赛、夺冠热门、最新前瞻文章入口
- `/schedule`              完整赛程（按日期 + 按小组两种视图）
- `/groups`                12 个小组一览（A–L）
- `/teams`                 球队列表
- `/teams/[slug]`          单支球队详情页
- `/players/[slug]`        单个球员分析页
- `/predictions`           预测板块（夺冠、晋级、单场比分预测）
- `/articles`              前瞻 / 球队介绍文章列表（Markdown 驱动）
- `/articles/[slug]`       单篇文章

## 五、数据约定（重要）
- 所有动态内容都来自 `data/` 下的结构化文件，模板只负责渲染：
  - `data/matches.json`    — 赛程与比分
  - `data/teams.json`      — 球队
  - `data/players.json`    — 球员
  - `data/groups.json`     — 小组与排名
  - `content/articles/*.md`— 前瞻/介绍文章（带 frontmatter）
- 字段先定义清楚、留好占位（如比分、预测结果），**真实数据由我本人填入，AI 不得编造**。
- 任何「待补充」的字段用 `null` 或明确的占位值，不要伪造看似真实的数字。

## 六、SEO 要求（这是流量命脉）
- 每个页面用 `generateMetadata` 输出独立的 `title` / `description` / Open Graph 标签。
- 自动生成 `sitemap.xml` 和 `robots.txt`。
- 比赛页面加结构化数据（JSON-LD，类型用 `SportsEvent`）。
- 语义化 HTML、合理的标题层级（h1 唯一）、图片带 alt。
- 移动端优先（mobile-first），保证 Core Web Vitals。

## 七、编码规范
- TypeScript 严格模式，所有数据结构定义明确的 type / interface。
- 组件可复用、目录清晰（`components/`、`lib/`、`data/`、`content/`）。
- 不引入需要付费 API key 或复杂后端的服务；保持纯静态可部署。
- 每完成一个阶段，确保 `pnpm build` 能成功通过。

## 八、明确禁止
- 不要伪造比分、阵容、预测等任何真实数据。
- 不要把内容硬编码进组件，必须走 `data/` 文件。
- 不要擅自更换技术栈或引入未约定的重型依赖。
- 不要一次性生成整站；按我给的阶段指令逐步推进。

## 九、世界杯 2026 关键事实（供你建模时参考，不等于数据）
- 时间：2026 年 6 月 11 日开幕 — 7 月 19 日决赛，共 39 天。
- 主办：美国、加拿大、墨西哥，16 座主办城市。
- 规模：48 支球队，104 场比赛。
- 揭幕战：墨西哥 vs 南非，墨西哥城 Estadio Azteca。
- 决赛：MetLife Stadium（New Jersey）。
- 赛制：12 个小组（A–L），每组 4 队；小组赛后，各组前 2 名 + 成绩最好的 8 个第三名，共 32 队进入淘汰赛；淘汰赛为 32 强 → 16 强 → 8 强 → 4 强 → 决赛。
> 注意：以上是赛制结构，用于设计数据模型；具体球队、分组、比分、时间以我提供的数据文件为准。
